{
  "name": "teclado.js",
  "version": "0.0.1",
  "description": "Simple virtual keyboard inspired by mobile keyboards.",
  "main": "./lib/cjs/index.js",
  "module": "./lib/esm/index.js",
  "files": [
    "lib/"
  ],
  "scripts": {
    "clean": "rm -rf node_modules && rm -rf .pnpm-store && rm -rf lib",
    "tsc": "rm -rf lib && tsc -p tsconfig.json && tsc -p tsconfig-cjs.json",
    "minify": "esbuild lib/esm/index.js --bundle --minify --outfile=teclado.min.js",
    "prepublishOnly": "pnpm run tsc",
    "test": "jest",
    "docs": "echo 'todo docs'",
    "build": "pnpm run tsc && pnpm run minify && pnpm run size && pnpm run docs",
    "release": "pnpm run build && npx changelogen --output --bump",
    "size": "size-limit",
    "gzip-size": "gzip-size teclado.min.js"
  },
  "teclado.js": {
    "type": "git",
    "url": "git+https://github.com/eduhds/teclado.js.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/eduhds/teclado.js/issues"
  },
  "homepage": "https://github.com/eduhds/teclado.js#readme",
  "devDependencies": {
    "@babel/core": "^7.23.7",
    "@babel/preset-env": "^7.23.7",
    "@babel/preset-typescript": "^7.23.3",
    "@size-limit/file": "^11.1.2",
    "@types/jest": "^29.5.11",
    "babel-jest": "^29.7.0",
    "changelogen": "^0.5.5",
    "esbuild": "^0.20.2",
    "gzip-size-cli": "^5.1.0",
    "jest": "^29.7.0",
    "size-limit": "^11.1.2",
    "typedoc": "^0.25.6",
    "typescript": "^5.3.3"
  },
  "size-limit": [
    {
      "limit": "10 kB",
      "path": "teclado.min.js"
    }
  ]
}
